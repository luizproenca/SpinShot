node_modules

# Environment variables — never commit real secrets
.env
.env.local
.env.*.local

# Firebase config — project-specific, not committed
google-services.json

# Google Play Store service account key — never commit real secrets
submit_to_playstore.json

# @generated expo-cli sync-2b81b286409207a5da26e14c78851eb30d8ccbdb
# The following patterns were generated by expo-cli

expo-env.d.ts
/android
/ios
# @end expo-cli

# Local build/config cache — regenerated, not committed
.expo/web/cache
supabase/supabase